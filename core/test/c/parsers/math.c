/*
 *  Generated by libtree-sitter
 */

#include <tree_sitter/runtime.h>

enum {
  symbol_expr,
  symbol_term,
  symbol_factor,
  symbol_number,
  symbol_variable
};

static const char *rule_names[5] = {
  "expr",
  "term",
  "factor",
  "number",
  "variable"
};

TSTree * ts_parse_math(const char *input)
{
  SETUP_PARSER();

  PARSE_STATES {
    case 1:
    {
      if (p.lookahead_sym == symbol_expr)
        SHIFT(2);
      if (p.lookahead_sym == symbol_term)
        SHIFT(4);
      if (p.lookahead_sym == symbol_factor)
        SHIFT(6);
      if (p.lookahead_sym == symbol_number)
        SHIFT(7);
      if (p.lookahead_sym == symbol_variable)
        SHIFT(7);
      if (p.lookahead_char == '(')
        ADVANCE(8);
      if (isdigit(p.lookahead_char))
        ADVANCE(10);
      if (isalnum(p.lookahead_char))
        ADVANCE(11);
      PARSE_ERROR();
      break;
    }

    case 2:
    {
      if (p.lookahead_char == '+')
        ADVANCE(3);
      ACCEPT();
      break;
    }

    case 3:
    {
      if (p.lookahead_sym == symbol_term)
        SHIFT(4);
      if (p.lookahead_sym == symbol_factor)
        SHIFT(6);
      if (p.lookahead_sym == symbol_number)
        SHIFT(7);
      if (p.lookahead_sym == symbol_variable)
        SHIFT(7);
      if (p.lookahead_char == '(')
        ADVANCE(8);
      if (isdigit(p.lookahead_char))
        ADVANCE(10);
      if (isalnum(p.lookahead_char))
        ADVANCE(11);
      PARSE_ERROR();
      break;
    }

    case 4:
    {
      if (p.lookahead_char == '*')
        ADVANCE(5);
      REDUCE(symbol_expr);
      break;
    }

    case 5:
    {
      if (p.lookahead_sym == symbol_factor)
        SHIFT(6);
      if (p.lookahead_sym == symbol_number)
        SHIFT(7);
      if (p.lookahead_sym == symbol_variable)
        SHIFT(7);
      if (p.lookahead_char == '(')
        ADVANCE(8);
      if (isdigit(p.lookahead_char))
        ADVANCE(10);
      if (isalnum(p.lookahead_char))
        ADVANCE(11);
      PARSE_ERROR();
      break;
    }

    case 6:
    {
      REDUCE(symbol_term);
      break;
    }

    case 7:
    {
      REDUCE(symbol_factor);
      break;
    }

    case 8:
    {
      if (p.lookahead_sym == symbol_expr)
        SHIFT(9);
      if (p.lookahead_sym == symbol_term)
        SHIFT(4);
      if (p.lookahead_sym == symbol_factor)
        SHIFT(6);
      if (p.lookahead_sym == symbol_number)
        SHIFT(7);
      if (p.lookahead_sym == symbol_variable)
        SHIFT(7);
      if (p.lookahead_char == '(')
        ADVANCE(8);
      if (isdigit(p.lookahead_char))
        ADVANCE(10);
      if (isalnum(p.lookahead_char))
        ADVANCE(11);
      PARSE_ERROR();
      break;
    }

    case 9:
    {
      if (p.lookahead_char == ')')
        ADVANCE(7);
      if (p.lookahead_char == '+')
        ADVANCE(3);
      PARSE_ERROR();
      break;
    }

    case 10:
    {
      if (isdigit(p.lookahead_char))
        ADVANCE(10);
      REDUCE(symbol_number);
      break;
    }

    case 11:
    {
      if (isalnum(p.lookahead_char))
        ADVANCE(11);
      REDUCE(symbol_variable);
      break;
    }
  }

  END_PARSER();
}
